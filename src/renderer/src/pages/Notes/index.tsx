export default function Notes() {
  return (
    <div>
      <h1 className="text-2xl font-bold" style={{ color: 'hsl(var(--foreground))' }}>
        Notes
      </h1>
      <p className="text-sm mt-1" style={{ color: 'hsl(var(--muted-foreground))' }}>
        Lore, quests, secrets, and session recaps.
      </p>
    </div>
  )
}
